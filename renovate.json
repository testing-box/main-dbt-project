{
  "extends": [
    "config:base"
  ],
  "regexManagers": [    
    {
      "fileMatch": ["packages.yml"],
      "matchStrings": ["revision: (?<currentValue>.*?) # renovate: depName=(?<depName>.*)"],
      "datasourceTemplate": "github-releases"
    }
  ],
  "ignoreDeps": ["dockerfile"]
}
